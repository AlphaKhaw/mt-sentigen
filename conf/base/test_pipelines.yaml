data_downloader:
  base_url: "https://datarepo.eng.ucsd.edu/mcauley_group/gdrive/googlelocal/"
  output_folder: "${hydra:runtime.cwd}/data/gzip"
  use_subset: false
  use_metadata: false

data_extractor:
  input_filepath: "${hydra:runtime.cwd}/data/mock/mock_data.json.gz"
  output_folder: "${hydra:runtime.cwd}/data/mock/"
  key_columns: ["rating", "text", "resp"]
  suffix: "extracted.json"
  expected_filepath: "${hydra:runtime.cwd}/data/mock/expected_data.json"
